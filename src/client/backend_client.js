// data structure: graph syntax (dot file) where each node has
// a JsonLogic rule with outputs for graph node to move to next?
// could even just be a list at first, or a map from node name to node

import g from './fake_graph'

// TODO massage for caller?
const LoadFlowgraph = () => {
    return g;
}

export default LoadFlowgraph;
